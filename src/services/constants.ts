export const constants = {
    apiEndPoints: {
        getSections: 'section-list.json?api-key={key}&limit={limit}&page={page}',
        getArticles: 'all.json?api-key={key}&limit={limit}&page={page}'

    },
    pageSizeList: [8, 50, 100, 300, 500, 700, 1000]
};
